const age = 23;
const hasId = true;
const isDrunk = false;


if (age >= 18 && hasId && !isDrunk){
    console.log("You can drive 🚗")
} else {
    console.log("You cannot drive 🚫")
}

const user = "projoy";
const password = "1234";

if(user === "projoy" && password ==="1234"){
    console.log("You are logged in")
} else {
    console.log("You are not logged in")
}