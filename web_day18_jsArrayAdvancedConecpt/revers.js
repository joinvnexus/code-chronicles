
const numbar =[1, 2, 3, 4,5];

const revers =[];

 for(let i = 0; i < numbar.length; i++){
     // console.log(numbar[i]);
       revers.push(numbar[i])
    //   revers.unshift(numbar[i])
 }

// for( let i = numbar.length-1; i >= 0; i--){
    
//     revers.push(numbar[i])
// }

// for(const num of numbar){
//     revers.unshift(numbar)
// }

//   console.log(numbar);
//   const result =numbar.reverse();
//   console.log(numbar)
//   console.log(result);
 console.log(revers);