const poem = `Of making many books there is no end,
   Though myriads have to deep oblivion gone;
Each day new manuscripts are being penned,
   And still the ceaseless tide of ink flows on.
 `
console.log(poem)

function sum (num1 , num2 ){
    const total = num1 + num2;
    const output = `The sum of ${num1} and ${num2} is ${total}`;
    console.log(output);
}
sum(10, 20);

const price = 100;
const discount = 20;
const totalPrice = `the total discount price is ${price - discount}`;
console.log(totalPrice);
