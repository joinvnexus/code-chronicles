//task one 

const fruits = ['apple', 'mango', 'orange' , 'bannana', 'pineapple'];

console.log(fruits[3]) 
fruits[2] = 'jambura';
console.log(fruits)

// task two 
const destinations = ['london', 'paris', 'dubai'];

// Add a new tourist destination to the array
destinations.push('new york');

// Add two more destinations to the array
destinations.push('tokyo', 'singapore');

// Remove the last destination from the array
destinations.pop();

// Display the final array
console.log(destinations);