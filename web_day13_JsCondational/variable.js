/**
 * variable.js ==> variable
 * 
 * 1. var
 * 2. let
 * 
 * 1. let
 * 2. const
 *
 */
//numbar
var sunglassprice = 80;
//string
var sunglassname = "sunglass";
//boolean
var isexpensive = true;

/**
 * naming convention
 * 1. camelCase
 * 2. snake_case
 */
var camelCase = 89;
var snake_case = 89;

/**
 * operation
 * 1. addition(+) 
 * 2. substraction(-)
 * 3. multiplication(*)
 * 4. division(/)
 */

var total = sunglassprice + 10;
var total = sunglassprice - 10;
var total = sunglassprice * 10;
var total = sunglassprice / 10;

/**
 * operation shortcut
 * 1. +=
 * 2. -=
 * 3. *=
 * 4. /=
 *
 */
var total = sunglassprice += 10;
var total = sunglassprice -= 10;
var total = sunglassprice *= 10;
var total = sunglassprice /= 10;